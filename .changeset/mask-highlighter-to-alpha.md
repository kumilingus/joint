---
"@joint/core": minor
---

highlighters.mask - build the mask shape via `CellView.toAlpha()`; remove `transformMaskChild()` and the `MASK_CHILD_ATTRIBUTE_BLACKLIST`, `MASK_REPLACE_TAGS`, `MASK_REMOVE_TAGS` properties
